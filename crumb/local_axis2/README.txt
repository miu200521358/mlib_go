使い方

1. exeをダブルクリック or コマンドプロンプトを実行して、そこにexeをドラッグ・アンド・ドロップ

2. 元となるモデルを黒い画面にドラッグ・アンド・ドロップ

3. 親指の爪が向いている軸方向を入力して Enter キーを押す
   - 爪が上を向いていたら Y 、前を向いていたら Z を入力

4. 元となるモデルと同じフォルダに "ローカル軸_{年月日時分秒}" 付きのファイルが生成される

miu200521358
