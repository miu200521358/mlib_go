# direnv: このリポジトリ専用コマンドを PATH に追加する。
# scripts 配下には標準コマンドと同名(tr)のスクリプトがあるため、
# 先頭追加(PATH_add)は避けて末尾へ追加する。
mlib_scripts_dir="$(expand_path ../mlib_skills/scripts)"
case ":${PATH}:" in
  *":${mlib_scripts_dir}:"*) ;;
  *) PATH="${PATH}:${mlib_scripts_dir}" ;;
esac
