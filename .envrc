# direnv: td/tv/ti 共通スクリプトと task-flow 用環境変数を設定する。

export CODEX_SKILLS_DIR="$(expand_path ../mlib_skills/skills)"
export CODEX_TASKS_DIR="$(expand_path ../mlib_skills/tasks)"
export CODEX_SUBSYSTEMS="mu_model_viewer,mu_motion_viewer,bone_baker,pmx_translator,vmd_sizing_t4,mu_tree_viewer,mu_vrm2pmx"

# 共通スクリプト配置先を PATH 末尾へ追加する。
codex_scripts_dir="$(expand_path ../../scripts)"
case ":${PATH}:" in
  *":${codex_scripts_dir}:"*) ;;
  *) PATH="${PATH}:${codex_scripts_dir}" ;;
esac
